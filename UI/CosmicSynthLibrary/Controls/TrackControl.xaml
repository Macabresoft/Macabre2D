<UserControl x:Class="Macabre2D.UI.CosmicSynthLibrary.Controls.TrackControl"
             x:Name="_trackControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Macabre2D.UI.CommonLibrary.Converters;assembly=Macabre2D.UI.CommonLibrary">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="_boolToVisibilityConverter" />
    </UserControl.Resources>
    <ScrollViewer DataContext="{Binding ElementName=_trackControls}"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Grid>
            <ItemsControl ScrollViewer.CanContentScroll="False"
                          ItemsSource="{Binding Editors}"
                          MaxWidth="2560"
                          MaxHeight="1600" />
        </Grid>
    </ScrollViewer>
</UserControl>